{
  "name": "tailwindless",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.less",
  "scripts": {
    "watch": "rimraf dist && chokidar \"src/**/*.less\" -c \"npm run watch:less\" --initial",
    "lessc": "lessc src/index.less dist/index.css --source-map",
    "build": "rimraf dist && lessc src/index.less dist/index.css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "chokidar-cli": "^3.0.0",
    "less": "^2.5.3",
    "rimraf": "^3.0.2"
  }
}
